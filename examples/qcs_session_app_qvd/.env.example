# Qlik Cloud tenant host, no protocol, e.g. my-tenant.eu.qlikcloud.com
TENANT_HOST=

# OAuth client of type "Machine to machine" from
# Management Console > Identity & access > API keys / OAuth clients
CLIENT_ID=
CLIENT_SECRET=
